/** Host Loader entry for the installed DexThemes browser plugin. */

/** Stable Cordis plugin name. */
export const name = 'dexthemes';

/**
 * DexThemes is client-only: Harness owns the theme runtime and Settings slot.
 * @returns {void}
 */
export function apply() {}
